import { defaultColors } from '../theme'

export default `
 .modal-close-button {
   cursor: pointer;
   font-size: 1.5rem;
   color: #1C1F21;
 }

 .modal-check {
   border: 3px solid ${defaultColors.green};
   border-radius: 50%;
   color: ${defaultColors.green};
   font-size: 6rem;
   padding: 0.7rem;
 }
`
